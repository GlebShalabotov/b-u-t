const initMenu = () => {
    console.log('initMenu');
    loadinIcons();
}

const init = () => {
    initMenu();
};

document.addEventListener('DOMContentLoaded', (event) => {
    init();
});